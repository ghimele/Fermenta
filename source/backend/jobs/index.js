
const jobs = [
  {
    name: 'program',
    timeout: '1s', // give program script time to run
    interval: '1s'
  }

];

// if (process.env.NODE_ENV === 'production') {

// }

module.exports = jobs;