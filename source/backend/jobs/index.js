
const jobs = [
  {
    name: 'program',
    timeout: '5s', // give program script time to run
    interval: '1000'
  }

];

// if (process.env.NODE_ENV === 'production') {

// }

module.exports = jobs;